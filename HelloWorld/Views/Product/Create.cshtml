@model HelloWorld.Models.Product
@{
    ViewData["Title"] = "Tambah Produk Baru";
}

<div class="p-8 w-full max-w-2xl mx-auto">
    <a asp-action="Index" class="inline-flex items-center text-sm text-gray-400 hover:text-blue-600 mb-6 transition-colors">
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
        </svg>
        Kembali ke Daftar
    </a>

    <div class="bg-white shadow-xl rounded-2xl overflow-hidden border border-gray-100">
        <div class="bg-gray-50/50 px-8 py-6 border-b border-gray-100">
            <h2 class="text-xl font-bold text-gray-800">Tambah Produk Baru</h2>
            <p class="text-xs text-gray-500 mt-1">Pastikan data nama dan harga sudah sesuai sebelum disimpan.</p>
        </div>

        <form asp-action="Create" method="post" class="p-8">
            <div asp-validation-summary="ModelOnly" class="mb-6 p-4 bg-red-50 text-red-600 text-sm rounded-lg"></div>

            <div class="space-y-6">
                <div>
                    <label asp-for="Name" class="block text-sm font-semibold text-gray-700 mb-2">Nama Produk</label>
                    <input asp-for="Name" class="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-500/10 focus:border-blue-500 focus:bg-white outline-none transition-all" placeholder="Contoh: Kain Cotton 30s" />
                    <span asp-validation-for="Name" class="text-red-500 text-xs mt-1"></span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label asp-for="Price" class="block text-sm font-semibold text-gray-700 mb-2">Harga (Rp)</label>
                        <input asp-for="Price" type="number" class="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-500/10 focus:border-blue-500 focus:bg-white outline-none transition-all" placeholder="0" />
                        <span asp-validation-for="Price" class="text-red-500 text-xs mt-1"></span>
                    </div>

                    <div>
                        <label asp-for="Stock" class="block text-sm font-semibold text-gray-700 mb-2">Stok Awal</label>
                        <input asp-for="Stock" type="number" class="w-full px-4 py-2.5 bg-gray-50 border border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-500/10 focus:border-blue-500 focus:bg-white outline-none transition-all" placeholder="0" />
                        <span asp-validation-for="Stock" class="text-red-500 text-xs mt-1"></span>
                    </div>
                </div>
            </div>

            <div class="flex items-center justify-end gap-4 mt-10 pt-6 border-t border-gray-100">
                <button type="submit" class="w-full md:w-auto bg-blue-600 hover:bg-blue-700 text-white px-10 py-3 rounded-xl font-bold shadow-lg shadow-blue-200 transition-all transform hover:-translate-y-0.5">
                    Simpan Produk
                </button>
            </div>
        </form>
    </div>
</div>